package g55803.samegame.model;

/**
 * Provides states to use in the Observer pattern.
 *
 * @author Nathan Furnal
 */
public enum State {
    STARTED,
    PLAY,
    UNDO,
    REDO,
    GAME_OVER,
    WON
}
